json.extract! employee, :id, :name, :name_ar, :position, :position_ar, :phone, :email, :language, :statement, :statement_ar, :contact_id, :created_at, :updated_at
json.url employee_url(employee, format: :json)
